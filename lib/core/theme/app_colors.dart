import 'dart:ui';

/// App color palette
class AppColors {
  AppColors._();

  // Primary / accent (yellow for CTAs and highlights)
  static const primary = Color(0xFFFACC15);
  static const primaryDark = Color(0xFFE6B800);
  static const primaryLight = Color(0xFFFCD84D);

  // Secondary
  static const secondary = Color(0xFFFF9800);
  static const secondaryDark = Color(0xFFF57C00);
  static const secondaryLight = Color(0xFFFFB74D);

  // Neutral colors
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFF57C00);
  static const info = Color(0xFF1976D2);

  // Text colors
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textDisabled = Color(0xFFBDBDBD);

  // Auth / Garage Owner UI
  static const accentIconBg = Color(0xFFFACC15);
  static const accentIconBorder = Color(0xFF212121);
  static const primaryButtonText = Color(0xFFFFFFFF);
  static const stepperActive = Color(0xFFFACC15);
  static const stepperInactive = Color(0xFFD1D1D6);
  static const inputBorder = Color(0xFFE0E0E0);
  static const inputFill = Color(0xFFF5F5F5);
  static const inputIcon = Color(0xFF9E9E9E);
  static const successCheck = Color(0xFF388E3C);

  // Service chip icon colors (sleek palette)
  static const serviceIconRed = Color(0xFFE53935);
  static const serviceIconGrey = Color(0xFF757575);
  static const serviceIconGreen = Color(0xFF43A047);
  static const serviceIconBlue = Color(0xFF1E88E5);

  /// Teal/blue for appointment card detail icons (calendar, clock, pin, phone)
  static const detailIcon = Color(0xFF26A69A);
}

